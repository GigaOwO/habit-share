# 習慣シェアボード（habit-share）仕様書

中間制作 MVP / これからのJavaScriptの教科書

---

## 1. 3行仕様（最低ライン）

- **何を作る**：曜日ごとの習慣を自分で決めて、こなしたらチェックし、その続き具合を友達と見せ合えるWebアプリ
- **画面**：1画面（今日のタスク ／ 習慣の登録 ／ 今週の達成 ／ みんなの今日）
- **最初の1機能**：習慣を曜日つきで登録すると、今日の曜日に該当するものが「今日のタスク」に出る

一文紹介（発表①で使う）：
> 「月・水はジム、火は英語みたいに自分で決めた習慣をこなして、続き具合を友達と見せ合えるアプリです。」

---

## 2. なぜ作るか

一人で決めた習慣は、守らなくても誰も困らないから続かない。
「今日やることが決まっている」＋「他人に見られている」の2つが揃うと続く、という仮説を検証する最小アプリ。

---

## 3. 設計の肝：「共有」をどこまで作るか

「友達と共有」を素直に作るとサーバー・DB・ログインが必要になり、期限内に完成しない。
そこで**共有を3段階に分解**し、今回は ① のみを実装範囲とする。

| 段階 | 内容 | 今回 |
|---|---|---|
| ① 他人の達成状況が同じ画面に並ぶ | 「みんなの今日」セクション。友達のデータは**固定のサンプルデータ** | ✅ やる |
| ② 自分の週間成績を共有テキストに変換 | コピーしてLINE等に貼る | ⭕ 余裕があれば |
| ③ サーバー保存で全員がリアルタイム同期 | API + DB + 認証 | ❌ 次にやりたいこと |

**発表では「友達のデータはデモ用の固定データです」と正直に言う。**
隠すより、「共有のうち、体験として効くのは"並んで見える"ことだと判断して、そこだけ先に作った」
という**設計判断として説明する**方が評価される。

---

## 4. 技術構成

| 項目 | 選定 | 理由 |
|---|---|---|
| ビルド | **Vite（vanilla テンプレート）** | `npm run dev` で即動く。保存したら即反映。授業の進め方と同じ |
| 言語 | 素の JavaScript（ES Modules, ES2015+） | 教科書の範囲。フレームワークは使わない |
| 保存 | ブラウザの localStorage | サーバー不要で、リロードしても消えない |
| 外部ライブラリ | 使わない（CDN も含む） | 説明できない依存を持ち込まない |

### セットアップ

```bash
npm create vite@latest habit-share -- --template vanilla
cd habit-share
npm install
npm run dev
```

生成直後に `counter.js` と `javascript.svg` は削除する（使わない）。

### ファイル構成

```
habit-share/
├── index.html      … 画面の骨組み
├── src/
│   ├── main.js     … 起動・イベント登録（組み立て役）
│   ├── state.js    … 状態と localStorage 保存・読み込み
│   ├── render.js   … 画面の描画
│   ├── friends.js  … 友達のサンプルデータ（固定）
│   └── style.css
├── package.json
└── SPEC.md
```

**Vite を使う理由**：ES Modules でファイルを役割ごとに分けられるから。
`state.js`（状態）と `render.js`（見た目）を分けておくと、どこを直せばいいか迷わない。

---

## 5. データ構造

```js
// 習慣（ルーティン）1件
{
  id: "1730000000000",   // Date.now().toString()
  title: "ジムに行く",
  days: [1, 3],          // 0=日 1=月 ... 6=土
  createdAt: "2026-09-05T12:34:56.789Z"
}

// 完了記録：習慣ID と 日付 を組み合わせた文字列の配列
done = ["1730000000000__2026-09-07", "1730000000000__2026-09-09"]
```

**完了記録をこの形にした理由**（説明ポイント）：
「いつ・どの習慣をやったか」は本来2次元の情報だが、`ID__日付` の1本の文字列にすると
配列に `includes()` するだけで判定でき、チェックのON/OFFも1行で書ける。

localStorage のキー

| キー | 中身 |
|---|---|
| `habit.routines.v1` | 習慣の配列（JSON） |
| `habit.done.v1` | 完了キーの配列（JSON） |
| `habit.name` | 自分の表示名 |

---

## 6. 画面レイアウト

```
┌──────────────────────────────────┐
│  習慣シェアボード      9/5(金)      │
├──────────────────────────────────┤
│  今日のタスク                       │
│  ☑ 英語の勉強                      │
│  ☐ ジムに行く                      │
│  （今日は予定なし、のときの表示もある） │
├──────────────────────────────────┤
│  今週の達成    8 / 12 （67%）       │
│  月 火 水 木 金 土 日               │
│  ●  ●  ●  ○  ・ ・ ・              │
├──────────────────────────────────┤
│  みんなの今日                       │
│  たくま   2/3                     │
│  ゆうき   3/3  🔥                  │
│  みお     0/2                     │
├──────────────────────────────────┤
│  習慣を追加                        │
│  [やること            ]            │
│  日 月 火 水 木 金 土  ← チェックボックス │
│  [ 追加する ]                      │
│  ⚠ エラー（あるときだけ）            │
│  ─ 登録済み一覧（×で削除）           │
└──────────────────────────────────┘
```

---

## 7. 機能仕様

| ID | 機能 | 受け入れ条件 | 優先度 |
|---|---|---|---|
| F-01 | 習慣を追加 | やること＋曜日を選んで追加 → 登録一覧に増える | 必須 |
| F-02 | 今日のタスク表示 | 今日の曜日に該当する習慣だけが並ぶ。0件なら「今日は予定なし」 | 必須 |
| F-03 | 完了チェック | チェックすると完了になる。もう一度押すと戻せる | 必須 |
| F-04 | 保存と復元 | リロードしても習慣と完了状態が消えない | 必須 |
| F-05 | 入力チェック | 不正な入力では追加されず、理由がその場に出る（§8） | 必須 |
| F-06 | 今週の達成 | 今週（月〜日）の「予定数 / 完了数 / 達成率」と曜日別の丸が出る | 必須 |
| F-07 | みんなの今日 | 友達3人ぶんのサンプルデータと自分が並ぶ。全部完了の人には 🔥 | 必須 |
| F-08 | 習慣を削除 | × で削除。関連する完了記録も消す | 任意 |
| F-09 | 共有テキストのコピー | 週間成績を文章にしてクリップボードへ | 任意 |

---

## 8. 入力チェック仕様（F-05）

| 項目 | ルール | NG時のメッセージ |
|---|---|---|
| やること | 空・空白のみは不可。20文字以内 | 「やることを入れてね」 |
| 曜日 | 1つ以上選ぶ | 「曜日を1つ以上選んでね」 |
| 重複 | 同じタイトルの習慣がすでにある場合は不可 | 「その習慣はもう登録されてるで」 |

- 判定前に `trim()` する
- エラー時は状態を一切変更しない
- 該当するエラーは全部まとめて出す

---

## 9. 実装順（1ステップずつ、途中で必ず動かす）

| Step | やること | 終わったら確認すること |
|---|---|---|
| 0 | Vite雛形＋掃除＋画面の骨組み | `npm run dev` で画面が出る |
| 1 | F-01 + F-02（メモリ上だけ） | 追加すると今日のタスクに出る |
| 2 | F-03（完了チェック） | チェックのON/OFFができる |
| 3 | F-04（localStorage 保存） | リロードしても消えない |
| 4 | F-05（入力チェック） | 空で押しても壊れない |
| 5 | F-06 + F-07（今週の達成・みんなの今日） | 数字が合っている |
| 6 | 見た目を最小限だけ整える | 読める・押せる |
| 7 | 通し確認 ＋ 説明の練習 | §11・§12 を埋める |

**Step 3 が終わった時点で「動くMVP」として成立している。** 以降は加点。

---

## 10. 仕上げチェックリスト（配布パートA対応）

- [ ] 最初から最後まで、エラーなく1周できる
- [ ] 入力が空のとき、警告が出て何も壊れない
- [ ] 見出し・ボタンの文言がわかる言葉になっている
- [ ] 受け入れたコードの1か所を説明できる（§11）

---

## 11. 「説明できる1か所」に選ぶ場所

**第一候補：完了記録の持ち方と `todayKey()`**

説明の骨子（30秒で言える形にしておく）：

1. 完了は「どの習慣を・いつやったか」の2次元の情報。素直に作ると入れ子の構造になって扱いづらい
2. そこで `習慣ID__2026-09-05` という**1本の文字列キー**にして配列で持った。
   判定は `includes()` だけ、ON/OFF は「配列に足すか、抜くか」だけで済む
3. 日付文字列を作るとき `toISOString()` は**使っていない**。あれはUTCに変換するので、
   日本時間の朝9時より前だと前日の日付になってしまう。だから年・月・日を自分で組み立てている

**第二候補：`render()` 関数**
状態（`routines` と `done`）が変わったら画面を作り直す、という1本のルールだけで動いている理由。

---

## 12. 30秒デモ台本（配布パートB対応）

| # | 項目 | 中身 |
|---|---|---|
| ① | 何を作った？ | 「月・水はジム、火は英語みたいに自分で決めた習慣をこなして、続き具合を友達と見せ合えるアプリです」 |
| ② | 動かす | 習慣を1つ追加 → 今日のタスクに出る → チェック → 今週の達成率が動く → リロードしても残ってる |
| ③ | 工夫／詰まったこと | 完了記録を `ID__日付` の1本のキーにした話（§11）。または、共有を3段階に分けて①だけ作った判断（§3） |
| ④ | 次にやりたいこと | サーバー保存にして、友達のデータを本物にする |

※ ②のデモ前に「友達のデータはデモ用の固定データです」と一言入れる。

---

## 13. 今回やらないこと

ログイン・認証 / サーバー・DB / 通知・リマインド / 習慣の編集 /
連続日数（ストリーク）の計算 / グラフライブラリ / React 等のフレームワーク
