# CLAUDE.md

プロジェクトルートに置く。Zed の Claude Code（ACP）が毎回読む前提。

---

## プロジェクト

「習慣シェアボード」— 曜日ごとの習慣を自分で決めて、こなしたらチェックし、
その続き具合を友達と見せ合うWebアプリ。学校の授業課題（中間制作MVP）。
詳細な仕様は `SPEC.md` を参照すること。

## 最重要ルール

**この課題の評価軸は「完成度」ではなく「自分のコードとして説明できるか」。**
速く大量に書くことより、私が理解できる形で少しずつ進めることを優先する。

1. **一度に1機能だけ実装する。** 頼まれていない機能を先回りして足さない。
2. **20行を超えるコードを書く前に、まず日本語で方針を3行以内で説明し、私の「OK」を待つ。**
3. 実装後は必ず「このコードで新しく出てきた文法・API」を短く説明する。
4. リファクタリングを勝手にしない。既存コードを変えるときは理由を先に言う。
5. 私が理解していなさそうな箇所には、聞かれる前に「ここ説明する？」と聞く。

## 技術制約（厳守）

- **Vite の vanilla テンプレート**。`npm run dev` で開発する
- 素の JavaScript（ES Modules, ES2015+）のみ。**TypeScript にしない**
- **外部ライブラリ・CDN・追加の npm パッケージは一切入れない**（devDependencies も vite だけ）
- React / Vue / Tailwind / CSSフレームワーク / 状態管理ライブラリは使わない
- ファイルは `SPEC.md` §4 の構成から増やさない

## 設計方針

- 状態は `state.js` の `routines`（習慣の配列）と `done`（完了キーの配列）の2つだけ
- 状態を変えたら保存して `render()` を呼ぶ。この流れ以外で画面を書き換えない
- `render.js` は状態を読んで描くだけ。状態を書き換えない
- `main.js` はイベントを状態の更新につなぐだけ。ロジックを書かない
- **DOM を状態の置き場にしない**（チェック済みかどうかを DOM から読み取らない）

## コーディング規約

- `const` を基本、再代入するものだけ `let`。`var` は使わない
- **ユーザー入力を `innerHTML` に入れない。** `createElement` + `textContent` を使う
- 日付キーは `toISOString()` を使わず、ローカル時刻から `YYYY-MM-DD` を自作する
  （UTC変換で日付がズレるため。理由をコメントに残すこと）
- 関数名は動詞から始める（`addRoutine`, `toggleDone`, `renderToday`, `loadState`）
- コメントは日本語で、**「何をしているか」ではなく「なぜそうしたか」**を書く
- 1関数は30行以内を目安にする

## 保存

- localStorage キー：`habit.routines.v1` / `habit.done.v1` / `habit.name`
- 読み込み時は `JSON.parse` を try/catch で囲み、壊れていたら空配列で始める

## 見た目

- 派手にしない。読める・押せることが最優先
- 色は3色まで（背景・文字・アクセント）
- チェック済みと未チェックが一目で区別できること
- アニメーションは不要

## やらないこと

ログイン / サーバー通信 / 通知 / 習慣の編集機能 / テストコード /
README の自動生成 / vite.config.js の設定追加（デフォルトのまま使う）
